# Image generation provider keys for the planf3 skill.
# Default provider is Gemini (gemini-3.1-flash) — set GEMINI_API_KEY.
# Only needed if you switch --provider openai.
GEMINI_API_KEY=
OPENAI_API_KEY=
